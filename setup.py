from setuptools import setup, find_packages

setup(
    name="watermark",
    version="1.0.0",
    description="Watermark control library for Microsoft Fabric",
    packages=find_packages(),
    python_requires=">=3.8",
)
